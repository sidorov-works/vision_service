# ============================================================
# requirements.dev.txt
# Development зависимости для macOS с Apple Silicon
# Использовать: pip install -r requirements.dev.txt
# ============================================================

# ------------------------------------------------------------
# MLX экосистема (только для Apple Silicon!)
# ------------------------------------------------------------
# Основной MLX фреймворк от Apple
# Версия 0.31.2 - стабильная на начало 2026
mlx==0.31.2

# MLX для работы с VLM моделями (требует mlx)
mlx-vlm==0.6.1

# MLX для LLM (некоторые модели используют)
mlx-lm==0.31.3

# Metal бэкенд для MLX (автоматически подключается, но явно указать не помешает)
# mlx-metal==0.31.2  # Обычно идет вместе с mlx

# Некоторые модели (например, lighton) требуют torchvision
torch==2.12.0
torchvision==0.27.0

# ------------------------------------------------------------
# API сервер (те же версии, что и в prod)
# ------------------------------------------------------------
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-multipart==0.0.29
python-dotenv==1.2.2
slowapi==0.1.9
setproctitle==1.3.7

# ------------------------------------------------------------
# Аутентификация (для локального тестирования с auth)
# ------------------------------------------------------------
python-jose==3.5.0
ecdsa==0.19.2
rsa==4.9.1

# ------------------------------------------------------------
# Обработка изображений
# ------------------------------------------------------------
Pillow==12.2.0

# ------------------------------------------------------------
# HTTP клиенты
# ------------------------------------------------------------
aiohttp==3.13.5
httpx==0.28.1

# ------------------------------------------------------------
# Логирование
# ------------------------------------------------------------
logger-utils @ git+https://github.com/sidorov-works/logger_utils@v0.4.0

# ------------------------------------------------------------
# Научные вычисления
# ------------------------------------------------------------
numpy==2.4.6
tqdm==4.67.3

# ------------------------------------------------------------
# Валидация
# ------------------------------------------------------------
pydantic==2.13.4

# ------------------------------------------------------------
# Процессный менеджер для локального запуска нескольких сервисов
# ------------------------------------------------------------
honcho==2.0.0

# ------------------------------------------------------------
# Дополнительные утилиты для разработки
# ------------------------------------------------------------
# Автоматическая перезагрузка при изменении кода (через uvicorn --reload)
watchfiles==1.2.0

# Для отладки асинхронного кода
ipython==8.29.0  # опционально