# ============================================================
# requirements.prod.txt
# Production зависимости для CUDA + Transformers
# Использовать: pip install -r requirements.prod.txt
# ============================================================

# ------------------------------------------------------------
# PyTorch экосистема (CUDA 12.4)
# ------------------------------------------------------------
# ВАЖНО: Используем специальный индекс для CUDA 12.4
--extra-index-url https://download.pytorch.org/whl/cu124

# PyTorch 2.5.1 - стабильная версия для CUDA 12.4
# Не используйте 2.3.1 - она не оптимизирована для новых GPU
torch==2.5.1
torchvision==0.20.1

# ------------------------------------------------------------
# Transformers и связанные библиотеки
# ------------------------------------------------------------
# Основная библиотека - версия 4.57.0 стабильна
transformers==4.57.0

# Ускоритель для multi-GPU (опционально, но полезно)
accelerate==1.13.0

# Безопасные тензоры (быстрее загрузка моделей)
safetensors==0.7.0

# transformers 4.57.0 требует tokenizers >=0.22, <0.23
tokenizers==0.22.0

# SentencePiece для некоторых моделей (T5, Llama)
sentencepiece==0.2.0

# Protocol Buffers - нужен для некоторых конфигураций
protobuf==5.29.0

# HuggingFace Hub - скачивание и кэширование моделей
huggingface_hub==0.36.2

# ------------------------------------------------------------
# Специфичные зависимости для моделей
# ------------------------------------------------------------
# Qwen2.5-VL требует эту утилиту для обработки изображений
qwen-vl-utils==0.0.10

# Flash Attention 2 (опционально - для ускорения на A100/H100)
# Раскомментируйте если есть совместимое GPU
# flash-attn==2.6.3

# ------------------------------------------------------------
# API сервер
# ------------------------------------------------------------
# FastAPI - современный веб-фреймворк
fastapi==0.136.1

# Uvicorn с расширениями (uvloop, httptools) для максимальной производительности
uvicorn[standard]==0.47.0

# Для парсинга multipart/form-data запросов
python-multipart==0.0.29

# Загрузка переменных окружения из .env
python-dotenv==1.2.2

# ------------------------------------------------------------
# Безопасность и утилиты
# ------------------------------------------------------------
# Rate limiting - защита от DDoS
slowapi==0.1.9

# JWT аутентификация (если REQUIRE_AUTH=true)
python-jose==3.5.0
ecdsa==0.19.2
rsa==4.9.1

# Меняет название процесса (удобно для мониторинга)
setproctitle==1.3.7

# ------------------------------------------------------------
# Обработка изображений
# ------------------------------------------------------------
# Основная библиотека для работы с изображениями
Pillow==12.2.0

# ------------------------------------------------------------
# HTTP клиенты (для возможных внешних вызовов)
# ------------------------------------------------------------
aiohttp==3.13.5      # Асинхронный HTTP клиент
httpx==0.28.1        # Альтернативный HTTP клиент

# ------------------------------------------------------------
# Логирование
# ------------------------------------------------------------
# Кастомный логгер
logger-utils @ git+https://github.com/sidorov-works/logger_utils@v0.4.0

# ------------------------------------------------------------
# Научные вычисления
# ------------------------------------------------------------
# NumPy - нужен для многих операций
numpy==2.4.6

# tqdm - прогресс-бары (полезно для отладки)
tqdm==4.67.3

# ------------------------------------------------------------
# Валидация данных
# ------------------------------------------------------------
# Pydantic v2 - валидация моделей запросов
pydantic==2.13.4
pydantic_core==2.46.4

# ------------------------------------------------------------
# Вспомогательные
# ------------------------------------------------------------
# Работа с YAML конфигами (если понадобится)
PyYAML==6.0.3

# Регулярные выражения (уже есть в стандартной библиотеке, но эта быстрее)
regex==2026.5.9